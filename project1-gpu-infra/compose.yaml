services:
  ai-api:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: ai-api
    ports:
      - "8000:8000"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    restart: unless-stopped
